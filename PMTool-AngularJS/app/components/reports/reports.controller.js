(function() {
    angular
        .module("NWT")
        .controller("ReportsController", , ['$scope', 'DataFactory', 'ToasterService', function($scope, DataFactory, ToasterService) {
            
            var projectId = $routeParams.id;
            var userId = credentials.id;

            
            

        }]);
}());